import { View, Text, Image } from "react-native";
import React from "react";

const CustomerProfileImage = (imageUrl: string) => {
  return <Image source={{}} />;
};

export default CustomerProfileImage;
